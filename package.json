{
  "name": "del-app",
  "private": true,
  "scripts": {
    "dev:web": "turbo run dev --filter=@del/web",
    "dev:mobile": "turbo run dev --filter=@del/mobile",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@10.7.0"
}
